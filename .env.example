# Server Configuration
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
# AZURE_OPENAI_API_KEY is typically passed via headers from the client, 
# but if the proxy needs to authenticate as a master principal, set it here.
# Currently the proxy forwards headers (except host).

# AWS Configuration
AWS_REGION=eu-central-1
# Credentials are usually picked up from ~/.aws/credentials or environment variables:
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_PROFILE=default

# If you need to assume a role:
# AWS_ROLE_ARN=arn:aws:iam::123456789012:role/YourRoleName
